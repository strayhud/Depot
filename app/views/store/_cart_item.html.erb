<% if cart_item == @current_item %>  <tr id="current_item"><% else %>  <tr><% end %>    <td><%= cart_item.quantity %>&times;</td><!-->    <td><%= link_to cart_item.title, :action=>"remove_item", :target_item=>cart_item.title %></td> -->    <td><%= link_to_remote cart_item.title, {:url => { :action => :remove_item, :target_item => cart_item.title}}, { :href => url_for(:action => :remove_item, :target_item => cart_item.title) }  %>       <td class="item-price"><%= number_to_currency(cart_item.price) %></td></tr>